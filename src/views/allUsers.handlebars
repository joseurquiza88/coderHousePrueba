{{!-- user controller --}}
<div class="container mt-5"> 
    <div class="row">
        <div class="col">
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <div class="container-fluid">
                    <a class="navbar-brand" href="/api/products">Volver</a>
                </div>
            </nav>
        </div>
    </div>
    <div class="row mt-4">
        <div class="col">
            <h2>Lista de Usuarios</h2>
            <ul class="list-group">
                {{#each users}}
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <div>
                            <span class="fw-bold">{{first_name}} {{last_name}}</span><br>
                            <span class="text-muted">Email: {{email}}</span><br>
                            <span class="badge bg-secondary">{{role}}</span>
                       </div>
                        <button type="button" class="btn btn-info btn-sm button-delete-user" id="{{_id}}">Eliminar</button>
                    </li>
                {{/each}}
            </ul>
        </div>
    </div>
</div>
<script src="/js/changeRol.js"></script>